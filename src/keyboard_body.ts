

class Keyboard {
    constructor(parent: HTMLElement{
        super(parent);
        const mainKeyboard = new Keyboard(this.node);
        newKeyboard.onSettings = () =>
            const settingsPage = new settingsPage(this.node);
    })
};


